# Recipy

MVP d'appli de planning de repas (React + Vite). Toutes les données (recettes, likes, planning, liste de courses) sont stockées localement dans le navigateur (`localStorage`) — pas de backend pour l'instant.

## Développement

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
